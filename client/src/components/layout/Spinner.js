import React, { Fragment} from 'react'

const Spinner = () => {
    return (
        <Fragment>
            <i className="massive spinner icon"></i>
        </Fragment>
    )
}

export default Spinner
